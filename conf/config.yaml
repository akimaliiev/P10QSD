# Global configuration
seed: 42

# Data configuration
data:
  tickers_file: "sp500_tickers.txt"
  start_date: "2015-01-01"
  end_date: "2024-12-31"
  raw_dir: "data/raw"
  processed_dir: "data/processed"

# Feature configuration
features:
  prediction_horizon: 5  # days after filing to measure return
  window_sizes: [5, 10, 20, 50] # used for price only prediction

# Model configuration
model:
  type: "random_forest"
  params:
    n_estimators: 200
    max_depth: 5
    min_samples_split: 10

# Training configuration
training:
  test_size: 0.2
  split_type: "time_series"

sec:
  identity: "sarthak.malla@mbzuai.ac.ae"
  forms: ["10-Q"]
  sections: ["part_ii_item_1a", "part_i_item_2"]
